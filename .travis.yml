language: ruby

cache: bundler

rvm:
- "2.4"
- "2.5"
- "2.6"
# - "2.7" # not working yet due to Sinatra/Mustermann kwargs compatibility

before_install:
- gem install bundler

script:
- bundle exec rspec
